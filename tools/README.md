# Tools

`score_portfolio.py` is a lightweight local utility for running the synthetic portfolio through the scoring model and regenerating sample outputs. It uses transparent weighted scoring and does not call external APIs.
